import React from 'react'
import {AppBar, Toolbar} from "@material-ui/core"

const Header = (props) => {
  return (
    <AppBar>
      <Toolbar>
        Arc Development
      </Toolbar>
    </AppBar>
  )
}

export default Header
